# Handwritten-Digit-Recognition-System
A machine learning app built with Python and PyTorch using a CNN model to recognize handwritten digits (0–9). Features a Streamlit UI for drawing or uploading digits for real-time prediction, with SQL used to store prediction results and user data for analysis.
